extends layout

block prepend title
  | #{ page.title + ' - '}

block content
    header
        h1 val antonini
        h2 full stack software developer
    section.resume!= typogr(page.html).typogrify()